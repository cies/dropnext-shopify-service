# Required Shopify app settings
SHOPIFY_APP_CLIENT_ID=your_shopify_app_client_id
SHOPIFY_APP_CLIENT_SECRET=your_shopify_app_client_secret
SHOPIFY_SCOPES=read_products,read_orders,write_webhooks,write_merchant_managed_fulfillment_orders,read_merchant_managed_fulfillment_orders
DSS_BASE_URL=https://your-dokploy-domain.example.com

# Optional core settings
OAUTH_REDIRECT_PATH=/oauth/callback
PORT=8080
# In Docker, the image defaults to 9999. If your PaaS injects an empty PORT=, the app uses 9999 so the reverse proxy still matches EXPOSE 9999.

# DSS stateless token map (shop|token pairs, comma-separated). An entry that is not such a pair refuses to boot.
# Example:
# DSS_SHOP_ACCESS_TOKENS=my-store.myshopify.com|shpat_xxx,other-store.myshopify.com|shpat_yyy
DSS_SHOP_ACCESS_TOKENS=

# IMPORTANT: Docker Compose env_file=.env merges like most dotenv parsers — if the same KEY appears twice,
# the LAST line wins. Do not paste a second "Optional monolith" block with MONOLITH_BASE_URL= (empty):
# it will wipe an earlier staging/production URL and the app will log `[monolith] MONOLITH_BASE_URL is unset`.

# Optional monolith (DropNext backend). Set this to your **REST mount root** (no trailing slash).
# Example staging: MONOLITH_BASE_URL=https://staging.dropnext.com/api/shopify-service/v1
# → DSS calls PUT https://staging.dropnext.com/api/shopify-service/v1/stores/api-key ,
#    POST …/orders , etc.
# Leave MONOLITH_API_PREFIX empty unless you split host vs prefix.
MONOLITH_BASE_URL=
# Path segment(s) inserted after MONOLITH_BASE_URL: {BASE}/{PREFIX}/stores/… Leave empty usually.
MONOLITH_API_PREFIX=
# Bearer token for DSS -> monolith HTTP (Authorization header); the monolith reads the same name. NOT the
# per-shop Shopify Admin token: those go in the JSON api_key body on PUT …/stores/api-key after OAuth.
# Letters, digits and -._~+/ only: anything else is not a valid bearer token on the wire.
DSS_TO_MONOLITH_API_KEY=change_this_to_a_long_random_secret
# Required bearer token for monolith -> DSS webhooks; the monolith reads the same name. Same alphabet as above.
MONOLITH_TO_DSS_API_KEY=change_this_too_to_a_long_random_secret

# Allow http:// MONOLITH_BASE_URL (local/dev only; needs DSS_MODE=DEV, PROD refuses the flag at startup).
DSS_ALLOW_INSECURE_MONOLITH=false


# Optional log shipping to Logflare, where the monolith's logs also land: with both of these set, a
# webhook can be followed across the two services by its trace_id. Both empty (the default) means
# stdout only, which is what a local run wants.
LOGFLARE_SOURCE_NAME=
LOGFLARE_API_KEY=
# Override only to ship at a local stub instead of https://api.logflare.app.
LOGFLARE_ENDPOINT=

# DEV or PROD (the default), the same switch as the monolith's MONOLITH_MODE. DEV logs one line per HTTP request
# (method, path, status; the trace id rides in the MDC; never the query string, which carries the OAuth code).
DSS_MODE=DEV
